import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.conversandoenelaulapps.app',
  appName: 'Conversando en el aula PPS',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
