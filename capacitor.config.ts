import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.despertadorgpspps.app',
  appName: 'Despertador GPS',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
