import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.despertadorporgps.app',
  appName: 'Despertador GPS',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
